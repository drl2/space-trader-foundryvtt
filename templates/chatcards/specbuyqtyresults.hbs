<div class="st-chatcard">
    <span style="font-weight: bold">{{rollType}}</span>
    <br />

    {{#each detailRolls}}
        {{localize (concat 'SPACE-TRADER.TRADEGOODNAMES.' this.item.Name)}}
        {{{this.rollHtml}}}
        <br /><br />
    {{/each}}

</div>